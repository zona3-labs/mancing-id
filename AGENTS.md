## Agent skills

### Issue tracker

Issues are tracked in Linear under team `ENG` and project `mancing-id` through the official Linear MCP integration. See `docs/agents/issue-tracker.md`.

### Triage labels

The tracker uses the five default canonical triage labels. See `docs/agents/triage-labels.md`.

### Domain docs

This repository uses a single-context domain documentation layout. See `docs/agents/domain.md`.
